---

### 🛡️ 2. O `.gitignore` Corrigido
*Adicionadas as regras estritas no bloco inferior para ignorar os repositórios unificados, customizados e a herança de TLPP, impedindo o tráfego de arquivos pesados da TOTVS para o Git.*

```text
# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# Editor/IDE
# .idea/
# .vscode/

# --- TOTVS Artifacts & Binaries ---
*.tar.gz
*.zip
*.tgz
dbaccess/dbaccess_linux_x64.tar.gz

# --- Environment & Secrets ---
.env

# --- Docker Persistent Data ---
.docker/

# --- TOTVS RPO Repositories (Strict Deny) ---
protheus/apo/tttm120.rpo
protheus/apo/tlpp.rpo
protheus/apo/custom.rpo